export const httpCodes: Record<string, number> = {
  OK: 200,
  UNAUTHORIZED: 401
}
export const httpHeaders: Record<string, string> = { AUTHORIZATION: 'Authorization' }
export const ACCESS_TOKEN: string = 'Access-Token'
export const COOKIE_PATH: string = '/'
export const COOKIE_DOMAIN: string = '.saber3d.net'
